name: Notify Discord on PR Merge to Master

on:
  push:
    branches:
      - notify-on-pr-merge

concurrency:
  group: merged-to-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  discord-notify:
    runs-on: ubuntu-latest
    steps:
      - name: Build commit URL
        id: commit_url
        run: |
          echo "url=${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Short SHA
        id: short_sha
        run: echo "value=${GITHUB_SHA::6}" >> $GITHUB_OUTPUT
      
      - name: Use it
        run: |
          echo "Commit: ${{ steps.commit_url.outputs.url }}"

      - name: PR number (empty on direct commits)
        id: pr
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          pr='${{ github.event.pull_request.number }}'
          [ -z "$pr" ] && pr="$(gh api "/repos/$GITHUB_REPOSITORY/commits/$GITHUB_SHA/pulls" --jq '.[0].number // empty')"
          echo "number=$pr" >> "$GITHUB_OUTPUT"

      - name: Get PR title
        id: pr_title
        if: steps.pr_number.outputs.number != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TITLE=$(gh api repos/${{ github.repository }}/pulls/${{ steps.pr_number.outputs.number }} --jq '.title')
          echo "Merged PR title: $TITLE"
          echo "title=$TITLE" >> $GITHUB_OUTPUT

      - name: Notify Discord
        uses: rjstone/discord-webhook-notify@v2
        with:
          webhookUrl: ${{ secrets.DISCORD_PR_WEBHOOK }}
          username: "MagBridge CI"
          avatarUrl: "https://raw.githubusercontent.com/mag-bros/mag-bridge/assets-v1.2/.github/assets/sukces.png"
          severity: info
          color: "#ba005b"
          title: "🙈 Pull Request merged into '${{ github.ref_name }}'"
          description: |
            **Author:** `${{ github.event.pusher.name }}`
          details: |
            📁 **PR Title:** ${{ steps.pr_title.outputs.title }}
            ✨ **PR Number:** ${{ steps.pr_number.outputs.number }}
            🔗 **Commit Reference:** [${{ steps.short_sha.outputs.value }}](${{ steps.commit_url.outputs.url }})
          footer: "MagBridge CI • ${{ github.event.head_commit.timestamp }}"
