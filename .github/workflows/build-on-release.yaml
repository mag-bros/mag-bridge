name: Build & Publish Desktop Releases

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build-and-publish:
    name: Build & publish ${{ matrix.label }} zip
    strategy:
      fail-fast: false
      max-parallel: 6
      matrix:
        include:
          # Linux x64
          - os: ubuntu-24.04
            label: linux-x86_64
            arch: x86_64
            eb_platform: --linux
            python_version: "3.13"
            node_version: "24"

          # Linux arm64
          - os: ubuntu-24.04-arm
            label: linux-arm64
            arch: arm64
            eb_platform: --linux
            python_version: "3.13"
            node_version: "24"

          # Windows x86_64
          - os: windows-2025
            label: windows-x86_64
            arch: x86_64
            eb_platform: --win
            python_version: "3.13"
            node_version: "24"

          # On PyPI, the latest published rdkit version is 2024.9.4, not 2025.3.6.
          # # Windows arm64
          # - os: windows-11-arm
          #   label: windows-arm64
          #   arch: arm64
          #   eb_platform: --win
          #   python_version: "3.13"
          #   node_version: "20"

          # macOS Intel (x86_64)
          - os: macos-15-intel
            label: macos-x86_64
            arch: x86_64
            eb_platform: --mac
            python_version: "3.13"
            node_version: "24"

          # macOS Apple Silicon (arm64)
          - os: macos-15
            label: macos-arm64
            arch: arm64
            eb_platform: --mac
            python_version: "3.13"
            node_version: "24"

    runs-on: ${{ matrix.os }}
    env:
      EB_PLATFORM: ${{ matrix.eb_platform }}
      ARCH: ${{ matrix.arch }}
      PYTHONUNBUFFERED: "1"

    steps:
      - name: Checkout
        # https://github.com/actions/checkout/tree/v6.0.2
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Derive app version from tag
        shell: bash
        run: |
          RAW="${GITHUB_REF_NAME}"

          # Extract first semantic version from string
          VERSION=$(echo "$RAW" | sed -E 's/.*[^0-9]([0-9]+\.[0-9]+\.[0-9]+(-[A-Za-z0-9._-]+)?).*/\1/')

          if [ -z "$VERSION" ]; then
            echo "❌ Could not extract version from '$RAW'"
            exit 1
          fi

          echo "⧗ Derived version: $VERSION"
          echo "APP_VERSION=$VERSION" >> "$GITHUB_ENV"

      - name: Set up Node.js
        # https://github.com/actions/setup-node/tree/v6.3.0
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f
        with:
          node-version: ${{ matrix.node_version }}
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Set up Python
        # https://github.com/actions/setup-python/tree/v6.2.0
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405
        with:
          python-version: ${{ matrix.python_version }}
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements-ci.txt

      - name: Install backend dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-ci.txt

      - name: Install system fpm (Linux ARM only)
        if: runner.os == 'Linux' && matrix.arch == 'arm64'
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ruby ruby-dev build-essential
          sudo gem install --no-document fpm
          echo "USE_SYSTEM_FPM=true" >> "$GITHUB_ENV"

      - name: Build backend & Electron app
        shell: bash
        env:
          PYTHONIOENCODING: "utf-8"
        run: |
          python scripts/environment.py build \
            --platform "${{ matrix.eb_platform }}" \
            --extra "--publish never -c.extraMetadata.version=${APP_VERSION}"

      - name: Package installer into single zip
        shell: bash
        run: |
          set -euo pipefail

          DIST_DIR="frontend/build/app"

          case "$RUNNER_OS" in
            "Linux")
              installer=$(find "$DIST_DIR" -maxdepth 3 -type f \( -name "*.AppImage" -o -name "*.deb" \) | head -n 1)
              ;;
            "macOS")
              installer=$(find "$DIST_DIR" -maxdepth 3 -type f -name "*.dmg" | head -n 1)
              ;;
            "Windows")
              installer=$(find "$DIST_DIR" -maxdepth 3 -type f -name "*Setup*.exe" | head -n 1)
              ;;
          esac

          if [ -z "${installer:-}" ]; then
            echo "❌ No installer found in $DIST_DIR for $RUNNER_OS"
            exit 1
          fi

          echo "⧗ Found installer: $installer"

          zip_name="magbridge-${{ matrix.label }}.zip"

          if [ "$RUNNER_OS" = "Windows" ]; then
            powershell -Command "Compress-Archive -Path \"${installer}\" -DestinationPath \"${zip_name}\""
          else
            zip -j "$zip_name" "$installer"
          fi

          echo "⧗ Created $zip_name"

      - name: Upload zip to GitHub Release
        # https://github.com/softprops/action-gh-release/tree/v2.6.1
        uses: softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe
        with:
          files: magbridge-${{ matrix.label }}.zip
        env:
          GITHUB_TOKEN: ${{ github.token }}
